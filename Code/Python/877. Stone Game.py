class Solution:
    def stoneGame(self, piles):
        # total piles are even
        # total number is odd
        # Alice can either choose the odd or even index
        # sum(odd) > sum(even) or sum(odd) < sum(even)
        # so she always win
        return True



